const path = require('path');

const config = {
    "listenOnPort": 3333,
    "certificatePath": {
        "crt": path.resolve(__dirname, 'sslcert', 'server.crt'),
        "key": path.resolve(__dirname, 'sslcert', 'server.key')
    }
}

module.exports = config;